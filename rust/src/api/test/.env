# Git user configuration
GIT_USER_NAME="Test User"
GIT_USER_EMAIL="test@example.com"

# Remote repository settings
REMOTE_NAME="origin"
REMOTE_AUTH_TYPE="HTTPS"

# Test directories
TEMP_DIR_PREFIX="git_test_"

# Authentication credentials (leave empty for no auth)
GIT_USERNAME=""
GIT_PASSWORD=""

# Test repository paths (optional, will be auto-generated if empty)
# LOCAL_REPO_PATH=""
# ORIGIN_REPO_PATH=""
# CLONE_REPO_PATH=""