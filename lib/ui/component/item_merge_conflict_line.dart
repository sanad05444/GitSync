import 'package:flutter/widgets.dart';

class ItemMergeConflictLine extends StatelessWidget {
  const ItemMergeConflictLine({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
